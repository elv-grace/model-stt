# references/  — HAND-VERIFIED verbatim transcripts (the ground truth)

One file per excerpt, named `<excerpt_id>.txt` (ids are in ../excerpts.json).
Produced by ear from the audio using ../protocol/transcription_protocol.md and
the per-excerpt worksheets in ../worksheets/. One utterance per line. Keep
fillers and false starts; mark non-speech in [brackets]; write numbers as words.

This is the ONLY file the scorer treats as truth. Do not paste from the subtitle
or the script.
